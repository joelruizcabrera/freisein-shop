parameters:
	level: 8
	phpVersion: 80100 # Override composer.json config.platform.php as we cant change it to php8.1 right now
	paths:
		- custom/static-plugins
	excludePaths:
	    analyse:
	        - custom/static-plugins/**/tests/*
	ignoreErrors:
#	    - "#Method [a-zA-Z0-9\\\\:\\(\\)]+ has parameter \\$[a-zA-Z0-9_]+ with no value type specified in iterable type array#"
